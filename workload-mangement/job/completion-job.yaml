apiVersion: batch/v1
kind: Job
metadata:
  name: busybox
spec: 
  completions: 10
  parallelism: 3
  template:
    spec:
      containers:
      - name: busybox
        image: busybox:1.36
        command: ["sh", "-c", echo 'hello k8s' && sleep 10]
      restartPolicy: Never
  backoffLimit: 4
