apiVersion: v1
kind: Service
metadata:
  name: deployment-svc-node
spec:
  type: NodePort
  selector:
    app: nginx
  ports:
  - targetPort: nginx-port
    port: 8080
    protocol: TCP
    name: http
