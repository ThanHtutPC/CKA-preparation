apiVersion: discovery.k8s.io/v1
kind: EndpointSlice
metadata:
  name: external-service-slice
  namespace: endpoint-lab
  labels:
    kubernetes.io/service-name: external-service

addressType: IPv4

ports:
  - name: http
    protocol: TCP
    port: 80

endpoints:
  - addresses:
      - 10.10.10.50