apiVersion: v1
kind: Service
metadata:
  name: nodeport
  namespace: service-lab  
spec:
  type: NodePort
  selector:
    app: nginx
  ports:
  - port: 80 
    targetPort: nginx
    nodePort: 30080
    name: nginx 
    protocol: TCP
