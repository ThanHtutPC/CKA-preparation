apiVersion: v1
kind: Pod
metadata:
  name: hostpath-example
spec:
  containers:
    - name: app
      image: nginx
      volumeMounts:
        - name: host-data
          mountPath: /app/data

  volumes:
    - name: host-data
      hostPath:
        path: /data/my-app
        type: DirectoryOrCreate
